import React from "react";
import {withStyles} from "@material-ui/core/styles";
import AppBar from "@material-ui/core/AppBar";

const NavBar = props => {
  return (
    <div>
      <AppBar position="fixed">Nav</AppBar>
    </div>
  );
};

export default NavBar;
